{
  "name": "bedrock-chatbot-lambda",
  "version": "1.0.0",
  "description": "Bedrock Chatbot Lambda Function",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "package": "zip -r function.zip dist node_modules"
  },
  "dependencies": {
    "@aws-sdk/client-bedrock-runtime": "^3.500.0",
    "@aws-sdk/client-dynamodb": "^3.500.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "@types/node": "^20.0.0"
  }
}
